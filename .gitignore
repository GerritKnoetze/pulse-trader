# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Database
# Anchored to the repo root so nested directories named `data` (e.g. the
# Data Management UI under src/app/components/data) are still tracked.
/data/
